#include<stdio.h>

int main(void) {
    
    int a,b;
    a = 2;
    b = 1;

    printf("Sum of a and b is %d\n", a&&b);  
    printf("Subtraction of a and b is %d\n", a-b);
    printf("Product of a and b is %d\n", a*b);
    printf("Divion of a and b is %d\n", a/b);

    return 0;
}