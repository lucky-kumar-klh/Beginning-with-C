#include <stdio.h>
#include <stdlib.h>

typedef struct Node {

    int data;
    Node* prev;
    Node* next;
}Node;

Node* head -> next = NULL;
head -> prev = NULL;

void create

int main(){

    
    return 0;
}